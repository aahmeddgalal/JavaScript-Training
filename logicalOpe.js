let dayOfMonth = 13
let weekday = "f riday"

if (dayOfMonth == 13 && (weekday == "Friday" || weekday == "friday")) {
    console.log("(Scary Face :) 'Sorry Im too lazy to get it from the keyboard')")
}
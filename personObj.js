// Objects challenges
const Person = {
    "name" : "John Doe",
    "age" : 30,
    "city" : "New York"
}

function logDate(person) {
    console.log(`${person.name} is ${person.age} years old and lives in ${person.city}`)
}

logDate(Person)